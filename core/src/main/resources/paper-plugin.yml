name: '${project.name}'
version: '${project.version}'
api-version: '26.1'
main: 'at.flauschigesalex.animated_holo.core.AnimatedHoloPlugin'
authors: [ "FlauschigesAlex" ]
website: https://modrinth.com/plugin/animatedholo

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    ProtocolLib:
      load: BEFORE
      required: false
      join-classpath: true